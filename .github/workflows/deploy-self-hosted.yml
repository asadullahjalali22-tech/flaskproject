name: Deploy Self hosted

on:
  push:
    branches: [ "main" ]

jobs:
  delploy:
     runs-on: self-hosted
     steps:
       - uses: actions/checkout@v4
       - run: |
          cd /home/app/flaskproject
          docker compose up -d --build
